# HomeoXNet 稳态网络模拟器：开发与优化记录

> 最后整理：2026-07-21
>
> 本文是 HomeoXNet 的历史开发记录，从内部站点仓库整理而来。
> 其中的目录路径（`apps/homeostasis/`）指向当时的内部仓库结构；
> 在本开源仓库中，对应 `backend/` 与 `frontend/`。
> 运维与部署相关章节（VPS、PM2、Nginx）已移除，仅保留设计与实现过程。

## 项目定位

Homeostasis 是面向医学与生理学教学的动态稳态网络模拟器。系统把心血管、神经—体液、肾脏—体液、呼吸—酸碱、代谢—血液等子系统组织成相互耦合的反馈网络。用户可在“干预控制台”改变参数，并通过“复杂稳态网络”“生命监测”和“临床病情评估”观察即时反应、慢性调节与失代偿。

项目在持续迭代中形成了几项核心原则：

- 突出参数间的反馈、代偿和跨系统影响，而非孤立参数演示。
- 桌面端强调深度操作，手机端强调快速观察，两端采用不同交互策略。
- 用节点运动、颜色、脉冲、稳定度、疾病震颤和灰化表达生理状态。
- 同时提供中英文界面、参数临床意义、综合征判断和使用说明。
- 对布局、疾病场景和边界值进行回归检查，避免视觉优化破坏模型逻辑。

## 资料范围与限制

本记录按时间交叉整理自：

1. 仓库中 `homeostasis/` 和 `apps/homeostasis/` 的完整 Git 历史（2026-06-26 至 2026-07-16）。
2. 当前 Codex 环境检索到的相关聊天，包括使用说明、移动端稳定度、疾病动画、节点拖拽、面板折叠/缓存和专利更新等任务。
3. 用户补充的两组 Claude 对话：2026-07-11 的阴阳启动布局，以及 2026-07-12 的控制台自适应列数与生命监测折叠。
4. ChatGPT 汇总文档《Homeostasis 稳态网络模拟器开发记录（2026-06-26 至 2026-07-16）》，主要补充早期 v4/v6/v7/v8/v9、门户部署、Pro 规划和运行架构讨论。
5. 当前代码及旧 README 中的 v15、v17、v18、v19 记录。

当前环境能读取 Codex 任务，但没有读取个人 ChatGPT 历史的连接器。因此本文中的聊天资料覆盖当前可检索的 Codex 记录和用户提供的 Claude 对话；未同步进 Codex 的独立 ChatGPT 对话尚未纳入。为降低遗漏影响，本文以 Git 历史作为完整时间主轴，以聊天补充需求背景、设计取舍和试错过程。

整理时把证据分为三层：聊天中的“需求/声称完成”、Git 中的“实际提交”、当前代码中的“仍然存在”。若当时没有完成浏览器实测，会明确保留该限制，不把聊天总结直接当成最终验证结果。

## 时间线总览

| 日期 | 阶段 | 主要进展 |
| --- | --- | --- |
| 2026-06-26 | 原型落地 | 前后端、生理引擎、网络视图、干预界面、生命监测与死亡弹窗 |
| 2026-06-27–28 | 密集 UI/性能迭代 | v4/v6/v7/v8/v9、Canvas 监测、渲染节流、缩放、居中、auto-fit、手机适配与门户入口 |
| 2026-06-29 | Pro 规划 | 独立 Homeostasis Pro 原型、模块化稳态引擎与知识产权路线 |
| 2026-06-30 | 应用整合 | 从 `homeostasis/` 迁移至 `apps/homeostasis/`，接入应用体系与访问记录 |
| 2026-07-03–04 | 教学与交互扩展 | 指南、可调面板、多列控制台、稳定度告警、临床评估、参数解释、手机跳转 |
| 2026-07-09–10 | 动态表达与治理 | 系统配色、网络运动、死亡灰化、防篡改、医学参考、活动日志、节点脉冲 |
| 2026-07-11 | 移动端与网络语义 | 手机稳定度 HUD、节点精修、手机参数弹窗、系统联动环、基于蓝噪声采样的阴阳启动布局 |
| 2026-07-12 | 模型与场景强化 | 1–4 列自适应控制台、监测默认折叠、低速档、场景回归、更多疾病、疾病震颤 |
| 2026-07-13–14 | 成果固化 | 更新专利交底、运行截图、算法流程图和新颖性规避分析 |
| 2026-07-14 | 架构澄清 | 明确 Browser、Nginx、Node/Express、会话 API 与仿真引擎的职责 |
| 2026-07-16 | 个性化工作台 | 面板折叠、拖拽恢复、偏好缓存、4K 自适应、节点/系统拖拽与布局重置 |

## 详细开发过程

### 2026-06-26：从模型原型到可观察系统

首个提交 `d706e7b` 建立了 Node.js 后端、模拟引擎、前端入口、模拟器页面和样式，形成多系统参数、干预滑块与网络展示的基本骨架。同日继续加入“生命监测”和死亡弹窗（`2739b67`），并多次调整代谢与血液及其他系统的坐标（`63247b8`、`7032c0e`、`995f762`）。

ChatGPT 资料进一步补充了首版产品要求：面向医学生，以心血管、肾脏、呼吸和神经—体液调节为核心，至少覆盖 20 个相互耦合参数；交互主循环被定义为“参数扰动—反馈调节—稳态恢复/失代偿”。参数随后扩展到血压、心率、每搏量、外周阻力、血容量、RAAS、血糖、K⁺、Hct、PaO₂、PaCO₂ 等，并形成控制台、复杂稳态网络和生命监测/解释区域的基本产品结构。

同日完成了可部署的前后端拆分和中英文入口。后端使用 Node.js/Express 提供静态资源、会话和 API，模型集中在 `simEngine.js`；浏览器负责控制、快照请求与可视化。这一架构后来继续沿用并扩展。

这一阶段先解决“能运行、能观察”，再通过几何调整提高网络可读性。

### 2026-06-27–28：从工程原型到公开界面

ChatGPT 资料将 6 月 27 日保存的连续修改标记为 v4、v6、v7、v8、v9。压缩包版本号不等同于 Git tag，但其中关键实现可以在 `d706e7b`、`c9cac8e`、`09e284b` 等提交内容中找到对应代码。

| 聊天版本 | 目标 | 主要处理 | Git/当前代码核对 |
| --- | --- | --- | --- |
| v4 | 信息架构重排 | 日志并入说明区，扩大生命监测区域，统一水印透明度 | 后续提交保留了水印和面板重排结果 |
| v6 | 性能优化 | 生命监测由 SVG 改为 Canvas；仿真 tick 约 190 ms，网络约 280 ms 节流 | `TICK_INTERVAL_MS=190`、`NETWORK_RENDER_INTERVAL_MS=280` 和 Canvas sparkline 可在历史代码中确认 |
| v7 | 缩放与高度调节 | 删除独立事件日志、降低最小缩放、加入网络高度拖拽 | 历史代码确认 `NETWORK_SCALE_MIN` 曾为 0.45，并存在滚轮/双指缩放与高度调节 |
| v8 | 初始居中 | 首次加载按面板和网络尺寸自动居中 | 后续 auto-fit 代码保留居中 placement 逻辑 |
| v9 | 包围盒 auto-fit | 根据节点与文字包围盒计算缩放和偏移；窗口/面板变化后重算；用户手动缩放优先 | `fitNetworkToPanel()`、`networkUserNavigated` 和强制重置逻辑可在 `09e284b` 历史版本确认 |

性能阶段的关键转变是：生命监测不再每次构建大量 SVG，而改用高 DPI Canvas 折线；复杂网络也不再跟随每个生理 tick 全量重绘。布局阶段则从“允许缩放”逐渐升级为“首次自动完整显示、保持居中、用户操作后不再强制覆盖”。

移动端随后加入独立安全系数。历史代码中可确认 `NETWORK_MOBILE_SCALE_SAFETY=1.75`、`NETWORK_DESKTOP_SCALE_SAFETY=1.15`，说明竖屏不是简单复制桌面缩放，而是为图例、触控和狭窄视口预留更大安全空间。

- 加入动态监测（`7d3deef`）。
- 增加水印、调整生命监测布局并清理日志（`c9cac8e`）。
- 主面板根据屏幕自动适配（`09e284b`）。
- 对齐中英文介绍（`fd64482`）。
- 增加手机使用说明（`3f673a0`）。
- 作者姓名、个人主页和站点门户改为可点击链接（`642bb61`、`ff8bd03`）。
- 两次微调英文 `Resp. & Acid-base` 标签坐标（`aea2bb2`、`48faecb`）。

旧 README 的 v15–v19 也对应这段迭代：删除重复手机提示框，把介绍改为中英文指南列表，增加手机帮助弹窗，修正关闭按钮居中，并为制作者姓名增加链接。

6 月 28 日同时完成门户整合和产品命名。ChatGPT 资料记录门户名称曾明确为“稳态网络：心肾肺调控模拟器”，入口文案强调血压、心率、血容量、RAAS、呼吸酸碱、血糖和电解质的多参数反馈，而非把产品描述为普通游戏。当前仓库历史也能看到 Homeostasis 入口与门户内容在这一时期开始合并。

### 2026-06-29：Homeostasis Pro / Pro Plus 规划

ChatGPT 对话记录了一条与线上稳定版平行的增强路线：建立独立 `/homeostasispro/` 测试版，避免与 `/homeostasis/` 的静态资源和 Nginx 路由冲突，并重新设计 UI、保留中英文与制作人链接。

规划中的 Pro 引擎把单体模型进一步拆分为状态模型、网络模型、多时间尺度更新、约束、稳定性与解释器，并提出 `computeStabilityIndex()`、`detectCompensationMode()`、`tracePerturbationPath()` 等能力，用于区分代偿、失代偿、崩溃和解释扰动传播路径。

这一阶段应标记为“原型与架构规划”，不能与当前 `apps/homeostasis/` 中已投入使用的功能等同。它的重要作用是推动后续专利策略和模型/UI 解耦思路，而不是证明全部 Pro Plus 模块已上线。

### 2026-06-30：并入统一应用目录

`314b214` 将项目从仓库根目录的 `homeostasis/` 原样迁移到 `apps/homeostasis/`，标志着独立实验原型进入网站应用集合。随后增加访问记录（`b28c258`），并继续完善网络访问日志。Git 保留了迁移前后的文件历史，因此本文同时追踪两个路径。

ChatGPT 汇总还引用了 6 月 30 日服务器访问日志：门户 `/homeostasis/` 出现 `simulator_entry`，运行页 `/homeostasis/simulator.html?lang=zh` 出现 `simulator_run`，且包含不同浏览器、分辨率和会话。这类日志证明了“门户入口—模拟器运行页”链路当时已在线使用；它补充的是部署/访问证据，不替代 Git 对源代码变更的证明。

### 2026-07-03：可调工作台与稳定度告警

- 增加详细指南 PDF，并允许调整干预控制台宽度（`bdb1ed8`）。
- 控制台变宽后自动切换双列（`0f5944b`）。
- 在网络/生命监测之间增加高度分隔线，在控制台/中央区域之间增加宽度分隔线。
- 生命稳定度低于 70 时显示黄色警示；低于 50 时转为更红、更快闪烁的危险提示，告警边框随面板尺寸变化。

聊天中还记录了桌面 Chrome 在 4K 屏幕上的卡顿诊断。排查逐步排除了单纯模拟循环和暂停状态，重点转向大面积半透明面板、`backdrop-filter`、阴影、持续合成及 Chrome 特定绘制路径。这促使后续视觉效果更重视合成成本和自适应刷新。

### 2026-07-04：临床语义、参数解释与移动导航

这是教学内容和交互最密集的一轮迭代。

#### 布局与操作

- 控制台约在默认宽度 1.5 倍时切换双列，在约 2.3 倍时切换三列（`90eb9a8`）；阈值在聊天中经过 2.2、2.5、2.3 多轮试调。
- 生命监测高度根据卡片实际底部和屏幕高度自动计算；用户手动拖动后尊重手动值。
- Guide 面板和“临床病情评估”列表分别可滚动。
- 桌面端增加空格键暂停/继续。

#### 临床病情评估

讨论和实现的判断逐步扩展到：

- 低血压/低灌注、低氧血症/组织缺氧。
- 呼吸性酸中毒、呼吸性碱中毒。
- 高钾、低钾、低血糖、高血糖/高渗状态。
- 高血压/容量负荷过多、乳酸性酸中毒。
- 心动过速、心动过缓。
- 低钠血症/稀释性水过多、GFR 降低。
- 代谢性酸中毒、代谢性碱中毒。

参数点击后不再只显示抽象反馈环路，而是在评估框下方显示参数介绍、主要输入、主要影响和临床意义；点击空白区域则恢复默认 Guide，以及急性、呼吸、慢性、极端状态四类环路说明。同轮修复乳酸下调可能变成负数的问题，在模型层加入下限和计算保护。

#### 教学与手机流程

- 增加视频说明（`f51a913`）、英文缩写及副标题优化（`77c40ba`、`4d9e914`）。
- 完善参数说明卡（`438d0ff`）。
- Guide 卡片可滚动；手机右下角增加半透明按钮，可在观察区和控制台间快速往返（`f1315aa`）。
- 同步更新中英文手机使用说明（`d2b5c9b`）。

### 2026-07-09–10：让网络状态“看得见”

- 为生理系统建立颜色语义并增加网络运动（`73bb68d`、`060f7bb`）。
- 稳态崩溃后网络灰化（`14c806a`）。
- 增加前后端防篡改与完整性保护（`ca4c492`）。
- 增加中文医学参考链接（`5d9d825`）。
- 后端记录用户日志和活动事件（`fb345f5`）。
- 节点增加脉冲效果（`0e952c6`）。

状态表达由单纯数值变化，扩展为曲线、颜色、明暗、运动和节律共同表达。

### 2026-07-11：移动端精修与阴阳布局试错

#### 手机端可读性

- 在网络左上角增加仅手机显示的生命稳定度分数和健康条（`a4c64ee`）。
- 放大手机节点和文字、扩展间距，并上移肾脏/体液与代谢/血液系统（`a6dec4f`、`dc1e28a`）。
- 手机点击节点打开参数控制弹窗（`b0c9c86`），减少长页面往返。
- 点击系统时显示同步环形反馈（`ec312f4`）。
- 调整微信内置环境中的自动 PDF 行为（`d181f84`）。

#### Claude 对话：阴阳启动动画

2026-07-11 的 Claude 对话补全了 `613b6e6` 之前的设计过程：

1. 第一版把心血管、代谢/血液、肾脏/体液强制放在左叶，把神经—体液和呼吸—酸碱放在右叶，并为中间 S 曲线和两个“鱼眼”留空。
2. 用户认为按系统强制分边不够均匀，要求优先形成漂亮、平衡的阴阳图，而非固定系统所属侧。
3. Claude 随后取消系统分边，改用 Mitchell best-candidate（蓝噪声）采样：每个节点从 30 个随机候选点中选择与已有节点距离最大的点。
4. 节点按相对中心的角度与起始点配对，以减少从阴阳起点飞向最终网络位置时的路径交叉；约 4 秒后落回真实布局。
5. 中间保留约 `0.12R` 的 S 形空带，两处鱼眼半径约 `0.20R`，节点中心同时避开圆周边缘。

当时 Claude 的数值验证使用全部 34 个真实节点，报告两叶分布为 16/18、无节点进入空带/鱼眼/圆外，最近邻最小距离约 37 px。由于浏览器截图工具持续超时，这一轮没有完成真实页面截图或完整浏览器动画验收，只完成了几何验证。随后相关实现以 `613b6e6` 提交；当前代码仍保留阴阳几何、蓝噪声候选采样和背景淡出逻辑。

在这之前，Codex 聊天还保留过一次效果不符合预期而完整回滚的阴阳方案。两段记录合在一起说明：阴阳布局并非一次完成，而是经历“按系统分边/视觉不理想 → 回滚 → 取消强制分边 → 均匀采样 → 正式提交”的过程。

### 2026-07-12：自适应工作台、慢速观察与疾病扩展

#### Claude 对话：控制台宽度与生命监测折叠

用户连续提出了三项相互约束的需求：

1. 控制台不能再固定为默认单列，应根据屏幕尺寸自动决定宽度和列数；同时网络区域不能因控制台变宽而留下过多无效空白或被挤压。
2. 取消原先最多三列且卡片被压窄的问题；超宽屏允许四列，英文参数卡仍需保持可读宽度。
3. MacBook Air 2560×1664 场景中，生命监测会压缩复杂稳态网络高度，因此桌面端应支持折叠，并建议默认折叠，但保留标题、时间、“已隐藏/(hidden)”状态和显示按钮；手机端保持原行为。

Claude 的完成总结称修改了 `app.js`、`style.css` 和 `simulator.html`，并给出 1920→2 列、2560→3 列、3840→4 列、超宽屏最多 4 列的预期。对 `7abb801` 和当前代码的核查确认：

- `CONTROL_PANEL_MAX_COLUMNS = 4`，单列目标宽度为 `CONTROL_COLUMN_TARGET_WIDTH = 340`。
- 控制台宽度计算会为复杂稳态网络保留空间，移除了导致三列卡片仍过窄的旧宽度上限。
- CSS 通过 `--controls-template` 接收完整的 `repeat(N, minmax(0, 1fr))`，支持 1–4 列。
- 桌面端 `monitorHidden` 初始为 `true`；折叠时隐藏仪表主体但保留标题栏，网络占用释放的高度。
- 标题栏提供中英文“已隐藏/(hidden)”和“显示/隐藏、Show/Hide”，手机断点下隐藏这些新增控件。
- 保留自动宽度初始化、窗口尺寸变化时重算、手动拖动调宽等既有能力。

Claude 当时只完成了计算、括号/连线和静态检查；临时浏览器工具中途断开，因此没有完成真实浏览器验收。上述功能随后与后端模型调整和场景回归脚本一并进入 `7abb801`（`ColsAutoAdjust|CollapseVitalMonitor`），所以“功能实际提交”有 Git 证据，但当时对各分辨率的具体列数仍属于设计预期，而非完整设备测试报告。

#### 同日其他优化

- 增加 `0.1×`、`0.25×` 低速档，便于观察反馈传播（`080837a`）。
- 新增场景回归脚本，并与自动列数、监测折叠一并提交（`7abb801`）。
- 增加更多疾病和回归场景（`5224254`）。
- 微调系统标签坐标（`be385ff`）。
- 节点控制卡扩展至所有平台（`49289a2`）。
- 再次完善手机说明（`88a808a`）。
- 疾病开始或切换时触发约 1.5 秒的短促运动；后续进一步改成突然偏移、不对称回弹与短暂不规则高频震颤，在保持小范围位移的同时表达疾病发生的“不适感”（`eb87f2e`）。

这一阶段把自动场景回归纳入开发，使新增疾病和 UI 调整不再完全依赖人工观察。

### 2026-07-13–14：专利材料同步

随着模型、界面和算法大幅更新，项目启动专利技术交底书重大修订：重新梳理技术方案和算法流程，生成网页状态截图与流程图，对比多件虚拟生理/虚拟患者相关专利并形成新颖性规避分析，最终输出 Word、PDF、交付说明和分析材料，同时保留旧版。

### 2026-07-14：运行架构澄清

ChatGPT 对话在这一天集中澄清“是否必须使用 Node.js”。结合当前代码，更准确的结论是：界面动画、节点渲染、滑块交互和监测绘图在浏览器执行，但权威仿真状态与反馈计算位于后端 `simEngine.js`；浏览器通过会话 API 获取快照并发送控制指令。

当前运行链路可概括为：

`Browser → HTTPS/Nginx → Node.js/Express → session API → simEngine → snapshot → browser rendering`

Node/Express 同时负责 3 小时会话 TTL、速率限制、健康检查、静态资源、访问/学习事件记录和门户子路径适配。因而它不只是可有可无的静态服务器；不过前端视觉与交互仍保持相对独立，后续可以继续提高 headless 模型测试能力和故障降级能力。

### 2026-07-16：个性化工作台

#### 面板折叠、恢复与缓存

`674e195` 集中完善桌面工作台：

- 控制台标题旁增加隐藏/显示按钮。
- 分隔线可拖到最左侧隐藏，也可从隐藏位置向右拉出。
- 低于最小宽度时呈抽屉效果，松手吸附回隐藏；超过阈值后恢复卡片自适应和列数变化。
- 双击控制台分隔线在“隐藏/自适应默认宽度”间切换。
- 双击网络高度分隔线隐藏/展开生命监测。
- `localStorage` 保存控制台宽度、网络高度和显示状态，下次访问恢复。
- 拖动、折叠期间，网络节点平滑适应可用空间。
- 修正 3840×2160 和不同系统缩放下的默认列数，避免 4K 屏错误恢复为单列。
- 分隔线提示和无障碍文本同步支持中英文“双击隐藏/展开”。

#### 可重排节点与系统

`b45774d` 增加桌面端个性化网络布局：

- 单击节点仍打开参数弹窗；移动超过 5 px 才进入拖拽。
- 可单独拖动节点，释放后保持位置。
- 拖动节点时只重算所属系统的欧氏质心，并平滑移动该系统标签。
- 可拖动系统标签，以相同偏移整体移动标签和所属节点，不改变内部几何关系。
- 节点始终位于标签上层，标签不阻断节点交互。
- 修改布局后显示 `Reset Layout`，可平滑恢复所有默认位置。
- 反复拖动和重置不累计几何误差。
- 手机端暂时禁用拖拽，保留节点点击弹窗和系统反馈，避免触摸滚动冲突。

### 2026-07-18：学习报告与医学 AI 解读

将 `homeostasis-test` 当日验证通过的学习报告功能整体晋升到正式版：

- 会话期间记录时间序列、生理指标、干预、场景状态和关键事件，生成可独立打开的交互式 HTML 学习报告。
- 下载时可选择普通报告，或“报告 + AI 医学解读”；AI 失败或超时会自动回退并下载普通报告。
- AI 输入使用假名化分析会话标识和压缩证据包，不直接发送用户身份；医学提示词要求基于记录证据分析学习行为、生理学与病理生理学，并固定输出 12 个教学章节。
- AI Markdown 经过安全渲染后附在时间顺序报告末尾；输出前会移除“好的”“收到”等寒暄，只保留从“1. 总体评价”开始的正式报告。
- 正式版通过独立的 `/etc/kuaiyu-site/homeostasis-ai.env` 连接腾讯云 TokenHub，并使用独立配额缓存；默认每位用户每日最多生成 3 份 AI 报告。
- 下载弹窗显示当日剩余次数、等待动画和“不再提醒”选项；配额按服务端假名化客户端标识记录，模型调用失败会返还次数。
- 保留隐藏的当日测试豁免入口：连续点击提示中的“用户”15 次后解除当日次数限制，并显示可手动关闭、3 秒自动关闭的提示。
- 尿量模型新增持续多尿导致容量丢失和稳定度下降的反馈，并区分孤立性少尿与伴低灌注/低 GFR 的高风险少尿；多尿、少尿医学说明归入“尿量”参数卡。
- 正式版保持 `/homeostasis/` API、服务名、日志和配额文件独立，不沿用测试版的页面标识或存储路径。

验证覆盖医学 AI 请求构造、固定章节与寒暄清理、配额扣除/返还/豁免、AI 路由、报告生成、接口限流、33 个未治疗场景、20 个救治场景、前后端语法检查，以及正式版浏览器交互流程。

同日已同步部署到 <https://www.kuaiyu.site/homeostasis/>；公网健康检查确认正式服务在线且医学 AI 已配置，正式版初始每日额度为 3 次。

#### 更换 AI 模型

正式版的模型配置只在服务器环境文件中维护，文件为 `/etc/kuaiyu-site/homeostasis-ai.env`。更换模型时登录服务器并编辑：

```bash
sudo nano /etc/kuaiyu-site/homeostasis-ai.env
```

修改模型名称，例如：

```env
HOMEOSTASIS_AI_MODEL=你的新模型名称
```

如果模型供应商同时要求更换接口或思考模式，可一并修改：

```env
HOMEOSTASIS_AI_ENDPOINT=新的接口地址
HOMEOSTASIS_AI_THINKING=enabled
```

保存后重启正式版服务，使新的环境变量生效：

```bash
cd /opt/homeostasis-simulator-api
set -a
source /etc/kuaiyu-site/homeostasis-ai.env
set +a
PORT=3002 FRONTEND_PATH=/var/www/homeostasis-simulator/frontend pm2 restart homeostasis-simulator-api --update-env
```

最后检查：

```bash
curl https://www.kuaiyu.site/homeostasis/api/health
```

返回内容中的 `medicalAiConfigured` 应为 `true`。健康检查只确认密钥和 AI 配置已加载，不会实际消耗 AI 报告额度；要确认新模型本身可用，再从页面生成一份 AI 报告。API 密钥只能保存在服务器环境文件中，不要放入前端代码、网页或 Git。

### 2026-07-18：报告与 AI 使用情况进入学习日志

- `report_download` 事件继续作为报告实际下载的权威记录，并新增 `aiQuotaExempt` 和 `aiQuotaRemaining`，可区分普通报告、成功包含 AI 的报告、请求 AI 后回退的普通报告，以及豁免状态下下载的报告。
- 用户通过隐藏入口获得当日次数豁免时，新增 `ai_quota_exemption_granted` 学习事件；只记录豁免结果、点击次数和配额日期，不记录 API 密钥。
- `homeostasis-learning-events.csv` 保持“一条学习事件一行”，新增 `reportDownloaded`、`aiRequested`、`aiIncluded`、`aiFallback`、`aiQuotaExempt`、`aiQuotaRemaining` 和 `quotaExemptionGranted`。
- `homeostasis-learning-summary.csv` 保持“一次页面学习会话一行”，新增报告下载次数、AI 请求/成功/回退、AI 报告数量、是否获得配额豁免及是否在豁免状态下下载等汇总字段。
- 两个 CSV 来自同一份 `homeostasis-learning-events.jsonl`，但粒度不同，因此不物理合并；日志导出脚本保留为按需执行，不再由 Windows 计划任务自动拉取。
- `node scripts/export_homeostasis_learning_logsRegression.js` 已验证报告下载、AI 成功和配额豁免事件能正确写入两种 CSV。
- 正式页面已部署带缓存版本号的日志代码；历史前台同步与计划任务实跑记录仅作留档，当前不再自动执行。

## 关键设计演变

### 从固定三栏到自适应工作台

页面从固定布局逐步加入横向/纵向分隔线、多列阈值、屏幕高度测量、面板折叠、自动吸附和本地偏好，最终形成“默认自适应 + 用户可覆盖 + 下次恢复”的桌面工作台。

### 从数值面板到多层反馈表达

`数值变化 → 生命监测曲线 → 稳定度告警 → 系统颜色 → 节点脉冲/运动 → 疾病震颤 → 死亡灰化`

这些效果分别承担正常反馈、风险升级、疾病发生和稳态崩溃的语义，避免所有变化只依赖颜色或数字。

### 从参数调节到临床推理

系统从参数与反馈环路，扩展到综合征判定、参数介绍、临床意义、医学参考和疾病场景，使用户能沿“干预—生理响应—综合征—临床解释”观察模型。

### 桌面端与手机端分化

桌面端强调可调宽度、多列控制台、面板折叠、键盘控制和拖拽布局；手机端强调稳定度 HUD、放大节点、参数弹窗、快速跳转和避免手势冲突。两端共享模型，但不强求相同交互。

## 当前结构

```text
apps/homeostasis/
├─ backend/
│  ├─ package.json
│  ├─ server.js               # 服务、访问/活动记录及运行保护
│  ├─ simEngine.js            # 模型、网络元数据、临床评估与疾病逻辑
│  └─ scenarioRegression.js   # 场景回归验证
├─ frontend/
│  ├─ index.html              # 应用入口
│  ├─ simulator.html          # 模拟器主页面
│  ├─ app.js                  # 渲染、交互、动画、布局与本地偏好
│  ├─ style.css               # 主视觉与响应式样式
│  ├─ layout-fixes.css        # 布局兼容和覆盖修正
│  └─ assets/homeostasis-guide.pdf
└─ README.md
```

## 验证与质量控制

- 前后端 JavaScript 语法检查。
- 节点间距、标签坐标、面板尺寸和响应式阈值检查。
- `scenarioRegression.js` 疾病与生理场景回归。
- 3840×2160、不同系统缩放及桌面/手机布局验证。
- Chrome、Edge、手机浏览器与微信内置环境差异诊断。
- 失败视觉方案先回滚，再以独立实现重新提交。

## ChatGPT 汇总中的过时结论修正

新增的 ChatGPT 文档对早期聊天整理很有价值，但它当时审计的是另一个旧仓库 `kuainegrito/Physiology_made_interesting`，不是当前工作区所连接的 `kuainegrito/my_website`。因此其中几项结论需要按当前证据更新：

- “Homeostasis 尚未进入 Git”已经不成立。当前仓库从 `d706e7b` 起保留了 2026-06-26 之后的 Homeostasis 历史，并在 `314b214` 中完成目录迁移。
- “当前只有 9 个骑行模拟器提交”只适用于文档当时检查的旧仓库，不适用于当前仓库。
- “7 月 16 日节点/系统拖拽仍待实现”已经过时；功能已在 `b45774d` 实现并提交，手机端随后按需求禁用拖拽。
- 文档把部分仿真核心描述为纯前端运行；当前源码表明权威模型位于后端 `simEngine.js`，前端负责交互、请求和渲染。
- v4/v6/v7/v8/v9 是聊天/压缩包版本标识，而不是正式 Git tag。本文将其作为需求和实现阶段名称，并用相邻提交与历史代码验证关键机制。

因此，ChatGPT 文档在本文中被视为“早期开发叙事和部署资料”，当前 Git 与源码则是判断最终实现状态的主要依据。

## Git 提交索引

以下为直接修改 Homeostasis 路径的提交，按时间排序。迁移前路径为 `homeostasis/`，迁移后为 `apps/homeostasis/`。

| 日期 | 提交 | 摘要 |
| --- | --- | --- |
| 06-26 | `d706e7b` | 初始应用 |
| 06-26 | `2739b67` | 生命监测与死亡弹窗 |
| 06-26 | `63247b8` | 代谢与血液坐标优化 |
| 06-26 | `7032c0e` / `995f762` | 大系统坐标优化 |
| 06-27 | `7d3deef` | 动态监测 |
| 06-27 | `c9cac8e` | 水印、生命监测布局、日志清理 |
| 06-27 | `09e284b` | 主面板自适应 |
| 06-27 | `fd64482` | 中英文介绍对齐 |
| 06-28 | `3f673a0` | 手机说明 |
| 06-28 | `642bb61` / `ff8bd03` | 作者与门户链接 |
| 06-29 | `aea2bb2` / `48faecb` | 呼吸/酸碱标签坐标 |
| 06-30 | `314b214` | 迁移至 `apps/homeostasis/` |
| 06-30 | `b28c258` | 访问记录 |
| 07-03 | `bdb1ed8` | 指南 PDF 与控制台自定义 |
| 07-03 | `0f5944b` | 控制台双列 |
| 07-04 | `90eb9a8` | 控制台三列阈值 |
| 07-04 | `f51a913` | 视频说明 |
| 07-04 | `77c40ba` / `4d9e914` | 英文缩写与副标题 |
| 07-04 | `438d0ff` | 参数说明卡 |
| 07-04 | `f1315aa` | Guide 滚动与手机自动跳转 |
| 07-04 | `d2b5c9b` | 手机说明更新 |
| 07-09 | `73bb68d` / `060f7bb` | 系统配色与动态网络 |
| 07-09 | `14c806a` | 稳态崩溃后灰化 |
| 07-09 | `ca4c492` | 防篡改 |
| 07-09 | `5d9d825` | 中文医学参考链接 |
| 07-10 | `fb345f5` | 用户日志与活动记录 |
| 07-10 | `0e952c6` | 节点脉冲 |
| 07-11 | `a4c64ee` | 手机生命稳定度 HUD |
| 07-11 | `a6dec4f` / `dc1e28a` | 手机节点字体与几何优化 |
| 07-11 | `b0c9c86` | 手机节点控制弹窗 |
| 07-11 | `ec312f4` | 系统同步环反馈 |
| 07-11 | `d181f84` | 微信环境 PDF 行为调整 |
| 07-11 | `613b6e6` | 阴阳启动布局 |
| 07-12 | `080837a` | 0.1×、0.25× 速度 |
| 07-12 | `7abb801` | 自动列数、监测折叠与回归脚本 |
| 07-12 | `5224254` | 更多疾病 |
| 07-12 | `be385ff` | 系统标签坐标 |
| 07-12 | `49289a2` | 全平台节点控制卡 |
| 07-12 | `88a808a` | 手机说明更新 |
| 07-12 | `eb87f2e` | 疾病节点震颤 |
| 07-16 | `674e195` | 控制台折叠、分隔线与偏好缓存 |
| 07-16 | `b45774d` | 节点/系统可重排布局 |

### 2026-07-18：正式版更换 HomeoXNet 标志

- 将模拟器顶部文字标题替换为 HomeoXNet PNG 标志，桌面和移动端按原设计尺寸的 2 倍调整。
- 移除语言切换对标题文本的覆盖，已部署至 `/homeostasis/`。

### 2026-07-18：会话报告信息层级与趋势筛选优化

- 移除“稳定度曾降至 0”汇总指标；会话 ID 不再占用汇总卡片，改为显示在报告副标题中。
- “交互式参数趋势”增加“仅显示有改变的参数”，以本次会话的实际参数变化集恢复曲线，并隐藏稳定度曲线。
- 有 AI 医学学习解读时，解读区置于干预时间线之后、参数起点/终点/极值汇总之前。
- `npm.cmd run test:reports` 已通过；报告前端已同步部署至 `/homeostasis/`，健康检查通过。

### 2026-07-18：主入口与副入口统一使用 HomeoXNet 标志

- 主入口和模拟器启动弹窗的中英文 HomeoXNet 标题均替换为居中 Logo，尺寸略大于原文字。
- 保留语言切换逻辑，已部署至 `/homeostasis/` 并完成线上入口核验。

### 2026-07-18：HomeoXNet 标志统一放大 1.8 倍

- 主入口、顶部标题和启动弹窗中的 Logo 均按当前尺寸放大 1.8 倍，并重新部署正式版。

### 2026-07-18：启动页介绍重排与移动端操作区压缩

- 模拟器内部顶部 Logo 恢复为 1.5 倍；外部主入口与启动弹窗 Logo 保持 1.8 倍，手机端内部 Logo 居中显示。
- 启动弹窗以全局稳态反馈机制开场，介绍区可滚动，视频链接独列于末尾，开始模拟按钮保持在下方。
- 手机端疾病大类与具体疾病选择框保持并排布局；已部署至 `/homeostasis/` 并完成线上资源核验。

### 2026-07-18：移除模拟器页顶部重复说明

- 移除模拟器主页顶部的中英文稳态反馈描述，保留详细指南和移动端使用说明入口；已部署至 `/homeostasis/`。

### 2026-07-18：医学 AI 因果分析与参数显示名约束

- 医学 AI 证据包新增 `parameter_name_dictionary`，并为干预与统计数据明确提供 `parameter_display_name`；中文报告使用中文参数名，英文报告使用英文参数名，不再以 `symp`、`vagal`、`co` 等内部代词充当正文参数名。
- 提示词改为先核对事实，再按“原发事件 → 生理代偿 → 学习者干预 → 系统变化 → 器官后果”分析；区分记录事实、时间关联、机制推断和证据不足，突出最关键错误、错误严重度、相关疾病辨析与重点加粗。
- 修正第 12 节 Markdown 标题格式，并限制无证据阈值、学习者意图和抽象滑块的临床药物类比。
- `npm.cmd run test:medical-ai` 已通过，含中文 `symp → 交感张力` 与英文显示名回归；本次仅完成本地修改，未部署。

### 2026-07-18：HomeoXNet 标志缩小 20%

- 正式版的首页、启动页与模拟器页 HomeoXNet 标志高度和最大宽度均在原有基础上缩小 20%，并保持桌面端与移动端比例一致。
- 静态样式已发布至 `/homeostasis/`；通过线上 CSS 内容与 SHA-256 核验确认部署文件一致。

### 2026-07-18：顶部说明链接并入制作人信息

- 将“详尽说明”移动至制作人信息之后；“手机版使用方法”紧随其后，仅在最大宽度 `860px` 的移动布局显示。
- 桌面端保持制作人信息与“详尽说明”同一行；移动端仅在空间不足时允许自适应换行。已发布至 `/homeostasis/`，并核验线上 HTML、脚本和样式资源。

### 2026-07-18：手机端跳转按钮返回页首

- 手机端右下角跳转按钮显示“↑”时，改为平滑返回页面最顶端；显示“↓”时仍跳转到干预控制台。
- 更新脚本缓存版本并发布至 `/homeostasis/`；已完成脚本语法、服务器文件校验和线上资源核验。

### 2026-07-18：医学 AI 长会话事实约束与动态压缩

- 提示词锁定事实来源：干预操作只取 `chronological_interventions`，终末/极值只取 `parameter_statistics`，趋势只取 `physiological_checkpoints`，防止模型反转干预方向或混用数值。
- 新增疾病挑战与自由探索模式分流，并将干预分为关键、次要、可忽略和无法判断；明确区分手动即时变化、后续趋势与终末累计结果。
- 对超过 10 项的长会话只详解最重要 4 项，其余逐项简述，限制第 6 节篇幅并保证第 7–12 节完整；增加低乳酸、酸碱、低氧、低血糖和 ADH 等过度推断约束。
- `npm.cmd run test:medical-ai` 已通过；已部署并重启 `homeostasis-simulator-api`，公网健康检查返回 `medicalAiConfigured: true`，服务器文件校验一致。

### 2026-07-19：低钾血症名称规范

- 将低钾血症疾病场景的中文名称规范为“重度低钾血症”；已完成源码全文检索核验，并随应用专用发布包部署至 `/homeostasis/`。

### 2026-07-19：手机端制作人信息与说明链接分行

- 手机端将制作人信息居中显示，并把“详尽说明”和“手机版使用方法”移至下一行；英文版进一步将制作人及学校、无括号邮箱、两个说明链接分为三行且全部居中。
- 已更新静态资源缓存版本并部署至 `/homeostasis/`；公网健康检查正常，线上页面命中 `20260719-mobile-creator-v1`，服务器文件与本地 SHA-256 一致。

### 2026-07-19：失衡机制视图合并至正式版

- 将已在 `homeostasis-test` 验证的“失衡机制视图 / Imbalance Mechanism View”合并到正式版：疾病、手动干预或明确稳态失衡时可按实际扰动起点及网络关系重排 34 个指标；视图中隐藏系统标签，并可通过“恢复网络视图”回到默认布局。
- 快照新增 `scenarioAffectedKeys`，使随机扰动的实际驱动节点可参与布局计算；加入独立布局引擎和 `test:layout`，覆盖全部疾病场景、单节点及随机扰动组合、节点边界、标签防重叠、重置与平滑视窗过渡。
- 正式版保留原有医学 AI、报告、配额和入口逻辑；布局动画锁定起点/终点的联合安全视窗，结束后再自动适配，避免每帧窗口自适应造成卡顿或节点被裁切。
- `npm.cmd run test:layout`、`test:scenarios`、`test:reports`、`test:rate-limits`、`test:medical-ai` 均通过；已部署至 `/homeostasis/` 并重启 `homeostasis-simulator-api`，公网健康检查正常，线上文件与本地 SHA-256 一致。

### 2026-07-19：血糖单位转换为 mmol/L

- 将血糖显示单位从 `mg/dL` 统一转换为 `mmol/L`：基准值 `90` 转为 `5.0`，预警范围 `60–180` 转为 `3.3–10.0`，危险范围 `25–500` 转为 `1.4–27.8`。
- 同步换算高低血糖风险阈值、风险幅度和数值上限，保持原有相对模拟行为不变；`npm.cmd run test:scenarios` 已通过，待随本次发布部署至 `/homeostasis/`。

### 2026-07-20：学习事件操作类型记录

- `homeostasis-learning-events` 的每条事件新增 `userActionType`：未加载疾病时记录为 `random`；成功加载疾病后记录为 `(疾病大类/具体疾病)`，并在重启会话后恢复为 `random`。
- 学习事件 CSV 同步新增 `userActionType` 列，便于按自由探索与具体疾病情景筛选；`node scripts/export_homeostasis_learning_logsRegression.js` 已通过。

### 2026-07-20：正式版与测试版学习统计合并

- 稳定版学习事件新增 `entryType`，固定记录为“正式版”；与测试版日志合并导出后，可在事件明细和会话汇总中按入口区分。

### 2026-07-20：五分钟无操作自动退出

- 模拟会话连续 5 分钟没有点击、键盘、触摸、滚动或表单操作时，前端记录 `idle_logout`、关闭后端会话并返回应用入口；任一用户操作会重置计时。

### 2026-07-21：乳酸酸中毒重新定义与酸碱三时间尺度模型

- 原 `metabolicAcidosis`（“乳酸性代谢性酸中毒”／“Lactic metabolic acidosis”）命名不规范，按 Cleveland Clinic 规范更名为 `lacticAcidosis`（乳酸酸中毒（A 型／缺氧性）），参考 <https://my.clevelandclinic.org/health/diseases/25066-lactic-acidosis>；场景不再直接强推 `lactate` 与 `bicarbonate`，改为只驱动缺氧（`paO2`、`tissueO2`），乳酸、HCO₃⁻ 与 pH 由反馈网络自行推导。
- 碳酸氢盐拆为快慢两池：血液缓冲（数分钟，强酸按约 1:1 滴定 HCO₃⁻）、肺代偿（数分钟，经化学感受器→通气→PaCO₂）、肾脏排酸与再生 HCO₃⁻（3～5 天，一轮模拟内近乎冻结）。急性期只能靠缓冲与通气，补碱只是买时间。
- pH 改为 Henderson–Hasselbalch 线性化式 `pH = -0.78·PaCO₂ + 0.60·HCO₃⁻`，删去原式中乳酸的直接项（与 HCO₃⁻ 消耗重复计算）；强酸只能通过消耗 HCO₃⁻ 影响 pH，即阴离子间隙的含义。
- 新增组织供氧低于约 65% 后的供氧依赖性拐点，区分 A 型乳酸酸中毒与应激性高乳酸。
- 实测：乳酸 1→7.8 mmol/L，HCO₃⁻ 24→17.1（约 1:1），PaCO₂ 34 mmHg（Winter 公式预测 33.6），pH 7.34。

### 2026-07-21：碱中毒致低钾的双向 H⁺/K⁺ 模型

- 原 `t.potassium` 中的 `+0.35*acidosis` 在碱血症一侧被截断为零，碱中毒对血钾完全没有影响，且被错误地包在 `renalActive` 内（跨细胞 H⁺/K⁺ 交换是数分钟的膜事件，与肾脏无关）；网络拓扑图中 `pH→K⁺` 的连线早已存在，数学却只实现了一半。
- 快相移出 `renalActive` 并改为双向 `-0.45*z.pH`，约每 0.1 pH 变化 0.2～0.8 mmol/L；慢相新增碱中毒的肾性排钾，把分布性改变转为真正的总体钾缺失；并以 `-0.16*z.potassium` 进入慢池闭环，解释低钾性代谢性碱中毒为何不补钾就难以纠正。
- 实测：过度通气 pH 7.55 / K⁺ 3.50；补碱 pH 7.51 / K⁺ 3.71；呼吸性酸中毒 pH 7.19 / K⁺ 4.75。呼吸性与代谢性之间的不对称由模型自行涌现。
- `hypoK` 在 pH>7.45 且 K⁺<3.8 时更名为“碱中毒相关低钾血症”，处置引导改为先纠正碱中毒而非只补钾；严重度以血钾确实下移为门控，避免误报。
- 配套重标定：DKA 与水杨酸中毒的 `bicarbonate` 驱动改为双池后重新标定（−1.80→−1.50、−1.40→−1.10）；COPD 急性加重死亡窗口由 230–300 s 调整为 190–300 s（肾性代偿回到 3～5 天尺度后不再在一轮内部分挽救 CO₂ 潴留者）。

### 2026-07-21：酸碱与血钾模型自测试版同步至正式版

- 上述改动由 `apps/homeostasis-test/` 同步而来。两侧 `backend/simEngine.js` 与 `backend/scenarioRegression.js` 在同步前逐字节一致，故整体复制。
- `frontend/app.js` 不能整体复制：正式版有 27 行特有内容（`DEFAULT_API_BASE` 路由、`app: 'homeostasis'` 埋点、`ai_quota_exemption_granted` 学习事件与配额字段、`internalEditionNotice` 空值保护），因此仅以补丁方式套用 9 行文案改动（新增 Cleveland Clinic 乳酸酸中毒参考链接，并改写 pH、HCO₃⁻、乳酸、血钾四项中英文说明）。
- 同步后两侧回归一致：33 个未治疗场景 + 21 组救援策略全部通过。

### 2026-07-23：疾病参考资料入口与医学 AI 提示词参考锚定

本轮把"疾病场景"与"权威资料"正式打通，中英文各自引用不同来源。

#### 疾病参考角标（中英文）

- 选择并载入具体疾病后，疾病下拉框右上角浮出一个可点击的绿色角标，新窗口打开该疾病的权威条目；中文显示"人卫资料 ↗"，英文显示"Reference ↗"。
- 中文引用**人卫智数疾病知识库**（`test.pmphai.com`），英文引用 **MSD Manual Professional Edition**（`merckmanuals.com/professional`）。选择 MSD 的理由是：与参数卡已在使用的 `msdmanuals.cn` 同属一套著作，每个条目都有独立的 Pathophysiology 段落，且全文开放。
- 33 个场景中 31 个有对应条目；`exercise`（生理探索）与 `random`（未知随机扰动）不显示角标。全部英文链接已在 2026-07-23 用 curl 实测返回 200。
- 单一数据源：链接表放在 `backend/simEngine.js` 的 `SCENARIO_REFERENCES`，由 `scenarioReference(name, lang)` 按语言解析，经 `meta()` 下发到前端，同时注入医学 AI 证据包。前端不再持有第二份副本，避免两边漂移。

#### 需要留意的映射取舍

MSD 的条目划分与人卫不完全对应，以下几项是折中选择：`hemorrhage` 与 `cardiogenicShock` 分别指向 Shock 总论与急性冠脉综合征并发症（MSD 无独立低血容量／心源性休克条目）；`hyperosmolar` 指向 Acute Complications of Diabetes Mellitus（美国站无独立 HHS 页）；`lacticAcidosis` 指向 Metabolic Acidosis（MSD 未单列乳酸酸中毒）；`statusEpilepticus` 指向 Seizure Disorders。若要逐病独立条目，可改用 StatPearls。

#### 医学 AI 提示词：参考锚定与机制事实表

系统提示词新增两个区块，中英文同时生效：

- **REFERENCE-ANCHORED REASONING**：证据包新增 `reference_library`，含条目标题、URL 与 `open_sections`。中文为 `mechanism_and_compensation_only`（人卫的临床表现／诊断／治疗为收费内容，只能据此讨论机制与代偿，不得据此给出诊断标准、药物或治疗方案）；英文为 `all`（MSD 全文开放，可一并引用诊断与处置推理）。第 5 部分须以 Markdown 链接引用条目，第 12 部分重申教学模型属性；`reference_library` 为空时禁止编造链接。
- **CANONICAL MECHANISM FACTS**：10 条经核对的教材参考事实，是证据包之外唯一允许使用的数值来源，且必须标注为"教材参考值"，不得与 `parameter_definitions` 的模拟器阈值混用。内容涵盖：休克五分型及其血流动力学判据（CI < 2.2、PCWP > 18、MAP < 65）；酸碱三时间尺度与呼酸代偿公式（急性每 10 mmHg 升 1 mmol/L，慢性 3～5，肾脏需 3～4 天）——并明确"一轮模拟内看不到肾脏代偿"是模型正确行为，不得判为学习者失误；代碱 20:1 比值；H⁺/K⁺ 双向交换与 DKA 血钾悖论（血钾正常不等于钾正常）；乳酸酸中毒 A/B 型须凭 PaO₂ 与组织供氧证据区分，纠正方向是恢复氧输送；混合型酸碱识别（水杨酸两阶段、大面积肺栓塞低 PaCO₂）；低氧阈值 PaO₂ < 60、SaO₂ < 90%、PaCO₂ > 50；电解质阈值与 SIADH 的等容量特征；少尿定义（< 400 mL/24 h、< 17 mL/h）与肾前／肾性／肾后区分；肾上腺危象的容量性低血压。
- 事实来源为人卫智数上述条目的公开段落，已逐条抓取核对（21 个条目）。

#### 篇幅与 token 预算

- 原"全文约 3000–3800 字"的模糊目标改为逐节字符上限（1:220 / 2:320 / 3:260 / 4:380 / 5:520 / 6:1100 / 7:280 / 8:260 / 9:300 / 10:260 / 11:240 / 12:140，合计约 4300），并规定超长时优先压缩第 5、6 部分，绝不删减第 7–12 部分。
- `HOMEOSTASIS_AI_MAX_TOKENS` 默认由 `5000` 提高到 `9000`，`.env.ai.example` 同步更新。原因：思考模式开启时推理 token 与正文竞争同一预算，4300 中文字符已接近 5000 token，第 12 部分存在被截断的风险。**服务器上的 `/etc/kuaiyu-site/homeostasis-ai.env` 需要同步改为 9000，否则环境变量会继续覆盖新默认值。**

#### 验证状态

- `backend/medicalAiRegression.js` 新增断言：中英文 `reference_library` 的来源、URL 前缀与 `open_sections` 各不相同；无对应条目的场景（`random`）返回空数组不编造链接；系统提示词包含参考锚定与十条机制事实；`configuration().maxTokens` 默认 9000 且可被环境变量覆盖。
- **本轮未执行 `npm run test:medical-ai` 等回归**：当前 macOS 开发机没有安装 Node.js。前端 `app.js` 已在浏览器中加载并执行到 `init()`（仅后端 fetch 失败），可确认无语法错误；后端三个文件只做了括号与模板字符串配平检查。**部署前必须在有 Node 的机器上跑 `test:medical-ai`、`test:scenarios`、`test:reports`、`test:layout`、`test:rate-limits`，并在浏览器实测中英文角标。**

### 2026-07-23：AI 报告新增第 13 部分「参考资料」，第 5 部分不再引用

在同日前一轮的基础上，把参考链接从正文引用改为独立的参考资料章节，中英文分别引用不同来源。

#### 报告结构变化

- 第 5 部分「相关生理学与病理生理学」**不再出现任何链接**，只讲因果链：原发环节 → 代偿方向 → 代偿为何有限或失败。
- 新增 **第 13 部分「参考资料 / References」**，用 Markdown 链接列出本次会话适用的权威条目。
- 第 12 部分教学安全声明**文字不变**。
- 原提示词三处「不得出现第 13 部分」「最后一行应属于第 12 部分」的硬约束相应改写：现在是 13 个部分，并把"结尾不得有多余内容"的护栏重新锚定到第 13 部分最后一条链接，护栏本身保留。

#### 链接的挑选与排序全部在后端完成

新增 `backend/referenceLinks.js`，保存 49 条参数级链接的中英文两套解析，以及参数 → 链接的映射。`medicalAi.js` 的 `referenceLibrary()` 负责组装：

1. 疾病条目排最前（来自 `simEngine.SCENARIO_REFERENCES`）；
2. 参数条目按该参数本次会话的偏离幅度排序，即"动得最多的参数最相关"；
3. 按 URL 去重，总数上限 8 条（`MAX_REFERENCES`）；
4. 中文报告把中文源（人卫智数、msdmanuals.cn、Mayo 中文页）排到英文源（Cleveland、MedlinePlus）之前；英文报告因为每一步都按语言解析，天然只含英文源。

提示词里模型只负责"照抄排版"：不得改写标题、缩短或补全 URL、合并、重排、增删。**这样 URL 编造在结构上不可能发生，而不只是被禁止。**

#### 英文链接来源

- 27 条 `msdmanuals.cn` 链接的英文版为同路径换主机到 `merckmanuals.com`，标签改写为英文；**24 条全部 curl 实测返回 200**。
- Mayo 的 4 条中文页去掉 `/zh-hans/` 即英文页；Mayo 对 curl 与抓取工具一律返回 403，改用真实浏览器逐条打开确认标题无误。
- Cleveland Clinic（7 条）与 MedlinePlus（9 条）本就是英文页，两个语言共用。按要求保留 MedlinePlus。

#### 报告渲染器必须同步改（否则链接不可点击）

`frontend/session-report.js` 的 `appendMarkdownInline()` 原本只识别粗体、斜体、行内代码，**没有链接分支**，Markdown 链接会以原始字符显示。现已新增链接分支，并加了两道校验：

- `new URL()` 解析且只接受 `https:`；
- 主机名白名单：`test.pmphai.com`、`www.merckmanuals.com`、`www.msdmanuals.cn`、`my.clevelandclinic.org`、`www.mayoclinic.org`、`medlineplus.gov`；
- 不通过则**降级为纯文本**（不静默丢弃），生成的 `<a>` 带 `target="_blank" rel="noopener noreferrer"`。

学习报告是可离线打开的独立 HTML，链接文字与 URL 又都来自模型输出，因此这里是模型输出的安全边界，不能只做协议判断。

#### 验证状态

- **已实测**：在真实浏览器 DOM 中运行新渲染逻辑的 9 个用例全部符合预期——3 个白名单域名正确生成带 `rel="noopener noreferrer"` 的链接；`javascript:`、`http:`、非白名单域名、`data:` URI 四种全部降级为纯文本，未生成任何 `<a>` 或 `<script>`；粗体与行内代码不受影响。
- **已实测**：`simulator.html` 加载后控制台只有预期的后端 fetch 失败，`session-report.js` 与 `app.js` 均无语法错误。
- **已实测**：24 条 merckmanuals.com 链接 curl 返回 200，4 条 Mayo 英文页浏览器确认。
- `backend/medicalAiRegression.js` 已补断言：中英文来源分离、英文报告不含中文源、中文报告中文源在前、去重、上限 8、无对应疾病条目时不编造、第 5 部分不引用、第 13 部分标题存在。
- ~~**仍未执行 `npm run test:medical-ai` 等回归**：本机无 Node.js。~~ **已于 2026-07-23 在 Windows 侧补跑**：`test:medical-ai`、`test:reports`、`test:scenarios`、`test:layout`、`test:rate-limits` 五项全部通过。仍待人工确认的只剩「实际生成一份中文和一份英文 AI 报告，确认第 13 部分链接可点击」（需真实 API 凭据，回归用的是桩数据）。

#### 已知重复，待后续合并

`frontend/app.js` 的 `TRUSTED_PARAM_LINKS` 仍在为参数卡服务，与新的 `backend/referenceLinks.js` 内容重复。本轮只改 AI 报告链路，未动参数卡以控制影响范围。**两份表必须同步维护**，后续应让前端改从 `meta()` 读取，删除前端副本。

## 后续建议

### 2026-07-22：README 运维说明前置与时间线整理

- 将正式版单独部署、本地启动和 AI API 切换说明移到文档前部。
- 明确后续开发记录按日期阅读，并保留原有历史内容与验证结论。

- 将临床阈值、参考来源和模型假设拆分为可版本化的模型文档。
- 为拖拽布局增加可选的显式“保存布局/恢复默认”，并评估与现有本地偏好合并。
- 扩大场景回归，覆盖疾病切换、极端干预、慢性恢复和边界值。
- 建立 Chrome/Edge、1080p/4K、桌面/移动端的统一性能基线。
- 若取得 ChatGPT 历史导出，可按日期补入本文，并标明新增来源和修订日期。

### 2026-07-31：关闭并删除 JSONL 自动拉取

- 已停止并注销本机 3 个 JSONL 自动拉取计划任务，删除仓库根目录的 `pull_logs_every_2h.bat` 与 `pull_logs_once.bat`；相关缺失的 PowerShell 拉取脚本也不再被引用。
- JSONL 导出脚本仍可手动执行；已复查计划任务、拉取入口文件和项目文本引用，均不再保留自动拉取入口。

### 2026-08-01：疾病选择合并为单一入口的二级菜单（自 homeostasis-test 移植）

原「选择疾病大类」+「选择具体疾病」两个联动下拉框合并为一个「选择疾病」按钮：
大类悬停即向右弹出具体疾病，手机端改为整宽面板 + 就地展开的手风琴。
功能与 `apps/homeostasis-test` 2026-08-01 版本一致。

**移植方式（重要）**

- `frontend/app.js` **按锚点逐处打补丁，而非从 test 整体复制**。
  两个孪生应用的前端已显著分叉（本应用 3789 行，test 5207 行，
  test 侧多出学习模式、课程、灰显节点等约 1400 行，这些**不应进入生产**）。
  共 9 个锚点，每处均校验"恰好匹配一次"后替换，脚本在任一锚点缺失或歧义时中止。
- 移植内容仅限：菜单模块（123 行，仅依赖 `TEXT`/`meta`/`isMobileLayout`，与学习模式无关）、
  `updateScenarioReferenceLink` 中的标签联动、交互锁定名单、双语文案、
  静态文案、按钮/点击外部/Escape 的事件绑定，以及 `simulator.html` 与 `style.css` 的对应改动。
- 两个 `<select>` 保留并隐藏，继续充当状态与事件总线；
  选中菜单项后依次派发 category/scenario 的 change，
  因此重新开始提示、报告记录、学习事件全部按原路径触发。

**移植中发现并修复的问题**

- `layout-fixes.css` 中手机端 `.scenario-picker` 仍是旧的两列 grid，
  且该文件在 `style.css` 之后加载因而胜出，导致菜单在手机上只有 163 px 宽。
  改为 flex 后实测 379 px 视口下菜单宽 334 px（整宽）。
  这是移植时才暴露的：test 侧此前已修正，prod 侧遗漏。

**未移植**

- 学习模式（追踪一条环路 / 完整控制台）、课程、灰显节点、右栏宽度手柄等
  test 侧功能**均未移植**，本应用行为除疾病选择外保持不变
  （已实测：无模式切换器、无引导面板、无节点灰显、34 节点 / 34 滑条照旧）。

**验证状态**

- `test:scenarios`（33 个未治疗挑战 + 21 组抢救）、`test:layout`、`test:rate-limits`、
  `test:reports`、`test:medical-ai`：**全部通过。**`frontend/app.js` 通过 `node --check`，
  两个 CSS 文件括号配平。
- 浏览器实测（本地）：8 个大类 / 33 个疾病；桌面端逐个大类均向右展开且在视口内；
  选中「急性失血性休克」后场景载入、按钮显示病例名、隐藏 select 同步、参考角标定位正确
  （`offsetParent=scenario-menu-wrap`，位于按钮右上）；
  手机端整宽面板 + 就地手风琴、选中「胰岛素过量致低血糖」成功、无横向溢出。
- 前端资源版本号提升为 `?v=20260801-disease-menu-v1`
  （`app.js`、`style.css`、`layout-fixes.css`；`index.html` 同步）。

### 2026-08-03：全量同步 homeostasis-test（时间架构、学习模式、回放控制台、四代生理修正）

本次不是打补丁，而是把公网版整体推进到测试版的当前状态。此前两者已相差数代：
公网版没有 `temporalModel.js` / `modelSchema.js` / `compensation.js`，
没有观察时间尺度镜头、学习模式切换与回放控制台，求解器仍是简单欧拉法；
`simEngine.js` 84 KB 对 158 KB，`app.js` 256 KB 对 353 KB。
差距的起点是 `d43829b`「Homeostasis 时间尺度优化尝试（失败）」——
当时的时间架构在公网版被回滚，测试版随后另起炉灶重建并通过全部回归，本次同步的是重建后的版本。

#### 同步方式

逐字节复制以下文件，而非手工移植：
后端 `simEngine.js`、`temporalModel.js`、`modelSchema.js`、`compensation.js`、
`medicalAi.js`、`medicalAiKimi.js`、`server.js`、`aiQuota.js`、`referenceLinks.js`、
`package.json`、`package-lock.json`、`.env.ai.example` 及八个回归脚本；
前端 `app.js`、`style.css`、`layout-fixes.css`、`simulator.html`、`index.html`、
`session-report.js`、`topology-layout.js` 及两个回归脚本、`assets/`。
**未复制**：`node_modules`、`logs`、`medicalAi.tencent.js.bak`，
以及测试版独有的 `TEMPORAL_ARCHITECTURE_EXPERIMENT_2026-07-24.md` 与 `ai-analysis-testkit/`（开发用工具，非运行代码）。
**未触碰**：本 README、`search_rw_diseases.py`、`to_do_list.txt`。

复制后仅有九行按环境改写，这是两个应用**唯一**允许的差异，已逐条核对：

| 文件 | 公网版 |
|---|---|
| `backend/server.js` | `service:'homeostasis-simulator-api'`；两处 `app:'homeostasis'`；水印上报路径 `/homeostasis/` |
| `backend/aiQuota.js` | 配额缓存 `homeostasis-ai-quota.json`；盐 `homeostasis-ai-quota-v1` |
| `frontend/simulator.html` | 删除「贵医内部版」提示（公网版即公网版，不能自指） |
| `frontend/index.html` | 同上 |

`frontend/app.js` 原本也需要改两处（API 前缀、学习日志的 `page.app`），
本次在测试版侧改成从路径推导（新增 `APP_SLUG`，并给 `internalEditionNotice` 加空值保护），
因此 **`app.js` 现在在两个应用中逐字节相同**——这是最大的一个文件，也是最容易漏改的一个。
`simEngine.js`、`temporalModel.js`、`modelSchema.js`、`compensation.js`、`medicalAi.js`、
`medicalAiKimi.js`、`style.css`、`layout-fixes.css`、`session-report.js`、`topology-layout.js`
同样已逐字节相同。

#### 公网版由此获得的能力

时间架构（34 个节点各自的时间常数、5 个隐藏慢池、QSS 求解器、观察时间尺度镜头）、
学习模式单一入口（基础/高级）、回放控制台、代偿与恶性循环分析、
模型透明度面板、AI 报告的恶性循环与时间尺度段落、疾病参考资料角标，
以及同日两条生理学修正的全部内容（碱血症呼吸代偿、碳酸氢盐尿、氧解离曲线肩部、
高黏滞度、容量过负荷肺淤血、渗透压 = 2×Na⁺+葡萄糖、尿量对血钠的符号纠正）。
详细说明见 `apps/homeostasis-test/README.md` 的 2026-08-03 两条。

#### 验证

- 公网版自身 `npm test`：**六项全部通过**（scenarios / temporal / learning-modes /
  model-transparency / layout / reports），其中 scenarios 覆盖 33 个未干预挑战与 29 套救治策略。
- 本地起公网版后端（新增 `.claude/launch.json` 的 `homeostasis-prod` 条目，端口 3009）实测：
  `/api/health` 返回 `homeostasis-simulator-api`；
  页面无「内部版」提示；镜头页签、学习模式、回放控制台俱在；
  网络 34 节点 81 边；控制台无报错；
  经 HTTP 走完一次会话（建会话→设 ADH→在日镜头推进 7.7 小时），
  血钠 137.3 与渗透压 279.5 自洽（应有 279.5）。
- 手机版页首实测：复杂网络模式 4 行、初学者模式 3 行，中英文均无标签裁切。

前端资源版本号提升为 **`?v=20260803-osm-sync-v1`**。
**需要 PM2 重启** `homeostasis-simulator-api` 才能加载新的后端模块（`deploy_portal.sh` 会做）。
无新环境变量；AI 配额缓存文件名未变，既有配额数据不受影响。

### 2026-08-19：AI 报告增加会话质量门槛——太简单的会话不再调用模型

**动机。** AI 报告按 token 计费，而最可能来点它的恰恰是最没有内容可分析的会话：打开页面、
拉一下滑块、下载。原有的三道防线（每日配额、每 IP 限流、必须有干预才能出报告）都不看
**会话本身有没有东西可分析**。对任何自建部署来说，这是账单与空报告之间新增的一道闸。

**新增 `backend/aiEligibility.js`（后端为准）。** 三项指标，全部以**真实秒**计量：

| 指标 | 默认阈值 | 环境变量 | 含义 |
| --- | --- | --- | --- |
| 独立操作次数 | 3 | `HOMEOSTASIS_AI_MIN_ACTIONS` | 按 `actionId` 去重。加载一个疾病场景是**一次**操作，哪怕它一次改了 6 个参数 |
| 观察时长 | 90 秒 | `HOMEOSTASIS_AI_MIN_WATCHED_SECONDS` | `timeScale.lensSeconds` 各镜头求和。帧追踪丢弃超过 2 秒的间隔，隐藏标签页与暂停都不计入 |
| 最长静默间隔 | 20 秒 | `HOMEOSTASIS_AI_MIN_QUIET_GAP_SECONDS` | 全程至少有一段不碰控件的时间。一次都没有，说明这些操作没有观察过任何后果 |

`HOMEOSTASIS_AI_ELIGIBILITY=off` 可整体关闭。四个变量已写入 `backend/.env.example`，
并在 `README.md` 的成本控制表（中英双语）中说明。

**为什么是真实秒而不是模拟秒。** 时间镜头把模拟时间压缩 0.1×～11520×，一次"三天"的会话
可能只看了八秒，而秒级镜头下四分钟的细看只推进 90 模拟秒。用模拟时长判断投入程度会同时
冤枉认真的人、放过刷子。

**刻意不做的两件事。**

- **不按"改了几个不同参数"拦人。** 只动一个变量恰恰是好的实验设计，AI 提示词自己就在夸
  "isolation of variables"。用参数多样性做门槛会精准误伤最该放行的那批学习者。
- **缺数据一律放行。** 报告里没有 `timeScale`、没有 `samples`、干预没有 `elapsed`，一律按
  "未知"处理、不参与判定。实现上踩了一个坑：`Number(null)` 是 `0` 而不是 `NaN`，最初的
  `finite()` 把"不知道观察了多久"算成"观察了 0 秒"并据此拒人；已显式挡掉 `null`，回归钉住。

**先看基线再集中操作的人不会被误伤。** 录制器时钟从**第一次干预**起算，镜头计时从**整个
会话**起算，两者之差就是"动手之前看了多久"，被当作第一个静默间隔计入。

**接入点。**

- `backend/server.js` 的 `/api/ai-analysis`：判定在 `aiQuotaStore.reserve()` **之前**，
  被拒的请求既不调用模型也不扣配额，返回 `403 {error:'ai_session_too_simple', eligibility}`。
  持有隐藏豁免的用户跳过门槛——那个手势本就是用来演示 AI 报告的。
- `/api/session/start` 随会话下发 `aiEligibility`（阈值 + 是否启用），前端据此判断，
  所以改环境变量时页面提示与后端执行会一起变。
- `frontend/app.js`：`evaluateAiEligibilityLocally()` 是后端逻辑的镜像，**只用于提前告知**。
  不达标时禁用「报告 + AI 医学解读」按钮并给出黄色提示条，用学习者自己的数字说明还差什么
  （中英双语）；「下载普通报告」完全不受影响。绕过前端时，后端 403 会被识别并给出对应提示。
- `report_download` 学习事件（若已开启统计）新增 `aiGate {eligible, reasons, actions,
  watchedSeconds, quietGapSeconds}`，供调阈值时参考。

**已知的口径取舍。** 「加载一个疾病场景 + 观察十分钟不干预」只算 1 次操作，会被第一条拦下。
自建部署若认为这属于正当用法，把 `HOMEOSTASIS_AI_MIN_ACTIONS` 调到 1 或 2 即可，不必改代码。

**验证。** 新增 `backend/aiEligibilityRegression.js`（已挂进 `npm run test:medical-ai`），
拒绝侧覆盖一次拖拽 / 11 秒内狂拉 4 次 / 只加载场景，放行侧覆盖正常三次干预 / 全程只动一个参数 /
先看五分钟基线再连续操作 / 日镜头三天会话，另有缺字段不拦人、空 body 不崩、阈值可调、`off` 可关闭。
`backend/medicalAiRouteRegression.js` 补了端到端断言：太薄的会话返回 403、没有到达模型、
没有扣配额，豁免用户用同一份报告仍返回 200。本仓库 `npm test` 七个套件与 `test:medical-ai`
四项全部通过，`node --check` 通过。浏览器实测在上游站点仓库完成（同一份 `app.js`）。

前端资源版本号提升为 **`?v=20260819-ai-gate-v1`**。**无需新增任何环境变量**，不设即用默认阈值；
重启后端即可加载新的 `aiEligibility.js`。
